version: 3

includes:
  entt: ./benchmarks/Entt.yml
  entityx: ./benchmarks/Entityx.yml
  ginseng: ./benchmarks/Ginseng.yml
  openecs: ./benchmarks/OpenECS.yml
  flecs: ./benchmarks/Flecs.yml
  pico-ecs: ./benchmarks/PicoEcs.yml
  gaia-ecs: ./benchmarks/GaiaEcs.yml
  snapecs: ./benchmarks/SnapECS.yml

tasks:
  default:
    - task: all
    - task: all:complex

  all:
    - task: entt:all
    - task: entityx:all
    - task: ginseng:all
    #- task: openecs:all
    - task: flecs:all
    - task: pico-ecs:all
    - task: gaia-ecs:all
    - task: snapecs:all

  all:complex:
    - task: entt:complex
    - task: entt:complex-runtime
    - task: entt:complex-stable
    - task: entt:complex-group
    - task: entityx:complex
    - task: ginseng:complex
    #- task: openecs:complex
    - task: flecs:complex
    - task: pico-ecs:complex
    - task: gaia-ecs:complex
    - task: snapecs:complex
